import { getTokenList } from '../config/tokenList'

/**
 * List of all supported tokens with initialized balances.
 * Token metadata is managed in config/tokenList.ts
 */
export const DEMO_TOKENS = getTokenList()
